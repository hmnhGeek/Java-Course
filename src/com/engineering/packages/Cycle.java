package com.engineering.packages;

public class Cycle {
    // the name of the public class should be same as that of the file.
}

// there can be multiple files in a file.
class MiniCycle {
    // however, this class can only be accessed within this package and not the outer
    // packages.
}
